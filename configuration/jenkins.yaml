# credentials:
# nodes

security:
  envInject:
    enableLoadingFromMaster: false
    enablePermissions: false
    hideInjectedVars: true

jenkins:
  globalNodeProperties:
    - envVars:
        env:
        - key: "FOO"
          value: "BAR"
