- job:
    builders:
    - shell: |-
        jenkins-jobs --log_level debug --conf jenkins_jobs.ini update jobs/
        curl -X POST --user aap:"1139e868a72b0a0ca1eec457e479cbb16b" $JENKINS_URL/configuration-as-code/reload
    name: 'JenkinsDeploy'
    project-type: freestyle
    scm:
    - git:
        branches:
        - '*/master'
        url: https://github.com/alter-al/jjb
        wipe-workspace: true
    triggers:
    - pollscm:
        cron: '* * * * *'
        ignore-post-commit-hooks: false
